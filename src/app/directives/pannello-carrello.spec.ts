import { PannelloCarrello } from './pannello-carrello';

describe('PannelloCarrello', () => {
  it('should create an instance', () => {
    const directive = new PannelloCarrello();
    expect(directive).toBeTruthy();
  });
});
